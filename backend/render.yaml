services:
  - type: web
    name: spotfreeze-api
    runtime: node
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: ADMIN_USERNAME
        sync: false
      - key: ADMIN_PASSWORD
        sync: false
      - key: SESSION_SECRET
        generateValue: true
      - key: FRONTEND_ORIGIN
        sync: false
    disk:
      name: spotfreeze-data
      mountPath: /opt/render/project/src/data
      sizeGB: 1
